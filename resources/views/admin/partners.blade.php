@extends('layouts.dashboard')
@section('title','Verifikasi Mitra')
@section('page-title','Verifikasi Mitra')
@section('page-subtitle','Tinjau profil dan dokumen usaha sebelum menyetujui mitra.')
@section('content')
<form class="mb-5"><select name="status" onchange="this.form.submit()" class="input max-w-xs"><option value="">Semua status</option>@foreach(['pending','verified','rejected','suspended'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></form>
<div class="space-y-4">@foreach($partners as $partner)<article class="card p-5"><div class="grid gap-5 lg:grid-cols-[1fr_auto] lg:items-center"><div class="flex gap-4"><span class="grid h-14 w-14 shrink-0 place-items-center rounded-2xl bg-indigo-100 text-xl font-black text-indigo-700">{{ str($partner->business_name)->substr(0,1) }}</span><div><div class="flex flex-wrap items-center gap-2"><h2 class="font-black text-ink">{{ $partner->business_name }}</h2><x-status-badge :status="$partner->verification_status" /></div><p class="mt-1 text-sm text-slate-500">{{ $partner->business_type }} • {{ $partner->city }}, {{ $partner->province }}</p><p class="mt-2 text-xs text-slate-400">{{ $partner->user->name }} • {{ $partner->user->email }} • {{ $partner->phone }}</p><div class="mt-3 flex flex-wrap gap-2">@forelse($partner->documents as $document)<span class="rounded-lg bg-slate-100 px-2.5 py-1 text-[11px] font-bold uppercase text-slate-600">{{ $document->document_type }} ✓</span>@empty<span class="text-xs text-rose-500">Belum ada dokumen</span>@endforelse</div></div></div><div class="flex flex-wrap gap-2"><form action="{{ route('admin.partners.update',$partner) }}" method="POST">@csrf @method('PATCH')<input type="hidden" name="verification_status" value="verified"><button class="btn-primary py-2">Setujui</button></form><form action="{{ route('admin.partners.update',$partner) }}" method="POST">@csrf @method('PATCH')<input type="hidden" name="verification_status" value="rejected"><input type="hidden" name="admin_notes" value="Dokumen belum memenuhi persyaratan."><button class="btn-danger py-2">Tolak</button></form></div></div></article>@endforeach</div><div class="mt-6">{{ $partners->links() }}</div>
@endsection
