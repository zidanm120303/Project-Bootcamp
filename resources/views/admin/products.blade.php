@extends('layouts.dashboard')
@section('title','Moderasi Produk')
@section('page-title','Produk & Katalog')
@section('page-subtitle','Moderasi produk sebelum tampil di marketplace.')
@section('content')
<form class="mb-5"><select name="status" onchange="this.form.submit()" class="input max-w-xs"><option value="">Semua status</option>@foreach(['pending_review','active','draft','rejected','inactive'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></form>
<div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">@foreach($products as $product)<article class="card overflow-hidden"><img src="{{ $product->image_url }}" class="aspect-[16/8] w-full object-cover"><div class="p-5"><div class="flex items-center justify-between"><span class="text-xs font-bold text-indigo-600">{{ $product->category->name }}</span><x-status-badge :status="$product->status" /></div><h2 class="mt-3 font-black text-ink">{{ $product->name }}</h2><p class="mt-1 text-xs text-slate-500">{{ $product->partner->business_name }} • {{ $product->location_city }}</p><p class="mt-3 text-lg font-black text-ink">Rp{{ number_format($product->price,0,',','.') }}</p><div class="mt-4 flex gap-2"><form action="{{ route('admin.products.update',$product) }}" method="POST" class="flex-1">@csrf @method('PATCH')<input type="hidden" name="status" value="active"><button class="btn-primary w-full py-2">Setujui</button></form><form action="{{ route('admin.products.update',$product) }}" method="POST">@csrf @method('PATCH')<input type="hidden" name="status" value="rejected"><button class="btn-danger py-2">Tolak</button></form></div></div></article>@endforeach</div><div class="mt-6">{{ $products->links() }}</div>
@endsection
